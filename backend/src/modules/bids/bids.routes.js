const express = require('express');
const router = express.Router();
const bidsController = require('./bids.controller');

// STUB: Add bids routes here
// router.post('/', bidsController.placeBid);

module.exports = router;
