const express = require('express');
const router = express.Router();
const authController = require('./auth.controller');

// STUB: Add auth routes here
// router.post('/login', authController.login);

module.exports = router;
