/**
 * TODO for Teammate 1: Implement inventory controllers
 */

const list = async (req, res, next) => {
  // STUB
  res.status(200).json({ message: 'inventory list stub' });
};

module.exports = {
  list,
};
