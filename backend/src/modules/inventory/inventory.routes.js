const express = require('express');
const router = express.Router();
const inventoryController = require('./inventory.controller');

// STUB: Add inventory routes here
// router.get('/', inventoryController.list);

module.exports = router;
