/**
 * TODO for Teammate 2: Implement auctions controllers
 */

const list = async (req, res, next) => {
  // STUB
  res.status(200).json({ message: 'auctions list stub' });
};

module.exports = {
  list,
};
